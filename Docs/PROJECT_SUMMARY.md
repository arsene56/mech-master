# 机械大师：项目交接摘要

> 更新日期：2026-09-22。供新对话快速接手使用。此文以本次对话的最终明确要求和当前仓库为准；早期仅回复“可以 / 接受”的问题原文未全部保留，因此不据此猜测额外承诺。若本文与旧文档冲突，先核对当前代码、生成数据和用户最新决定。

## 1. 一页结论

- 项目名称：**机械大师**；Slogan：**拆解万物，解锁机秘**。
- GitHub 仓库：<https://github.com/arsene56/mech-master>。先前给出的 `study-time` 链接是错误地址，不应再使用。
- 本地工程：`D:\Code\Mech.Master`；直接在 `main` 分支开发，**由项目所有者自行提交和推送**。不要代为提交。
- 目标平台是**微信小游戏**，不是普通网页或 Java 应用。技术基线为团结引擎 / Unity 2022 LTS、C# 运行时、Blender 4.5 LTS 建模、.NET 8 规则测试。尚无微信小游戏 AppID。
- 产品面向约 6–12 岁的儿童与青少年机械科普；模型写实仿真，界面可以更明亮、适合青少年，但机械结构不可卡通化。等级只叫**简单 / 进阶 / 探索**，不在等级名称或 README 的等级表里绑定年龄段。
- 首个且目前**唯一正式入库**的机械模型是无品牌 **27.5 英寸、2×10、M 码铝合金硬尾山地车**。Source 具有 **14 模块、195 类零件、595 个独立实体单元**；交互步骤为 **15 / 30 / 45**。三档都覆盖同一批 595 个实体，只改变每次拖动的分组粒度。
- 原先尝试的 F1 简化模型及 `Docs/Preview/FormulaCar2026.png` 不达写实要求，F1 方向已放弃；仓库当前没有 F1 正式模型或菜单入口。OM10 腕表机芯、Tomaso V8 爆炸发动机仍只是候选，**尚未取得并审计原始 3D 文件，不得声称已接入**。
- 当前项目能在本机团结引擎 Editor 运行自行车样片，资产与规则验证已通过；**微信导出、分包、真机性能和触摸验收均未完成**，不要称为微信已上线或性能达标。

## 2. 产品目标、范围和底线

### 2.1 核心体验

玩家能自由旋转、缩放和平移写实 3D 机械，点选具体零件查看中文名称、作用与原理；通过直接拖动把零件或子总成拆到对应分类展示区，再从展示区拖回组装。拆下的部件不能凭空消失，应在 3D 收纳区域分类摆放；接近对应底部分类槽时，槽位变色提示，松手提交。无论拆解还是组装，都允许**任选当前可操作单元**，不设强制顺序。

“1:1”指**源模型严格采用真实物理尺寸和比例**，运行时 1 Unity unit = 1 m；屏幕上允许自由缩放观察，并非要求在手机屏幕按现实尺寸显示。工程精度目标是可信的尺寸、接口、层级和拆装关系（维修训练级），不宣称原厂制造公差、材料应力、有限元或真实油液模拟。

首版只做自行车，但框架应让后续合格模型以独立清单、目录和资源接入，不再修改主运行逻辑。长期可考虑非电动汽车、燃油发动机、火车、飞机、轮船和钟表机芯；用户明确说**不要电动车**。第二模型的步骤数应依真实可拆结构确定，不机械套用自行车 15 / 30 / 45，也不虚构零件凑数。

### 2.2 明确排除

- 不加入星级、徽章、排行榜、连续登录、扣分惩罚、广告或付费抽取；重点是科普和拆装乐趣。
- 不把工具型号、规格、扭矩作为关卡门槛。界面历史文案“手”已改为“无需工具”；当前操作是**直接拖动**，工具类别仅可保留在 BOM 中供现实知识说明。内部旧枚举仍可能包含 `ToolKind` / `WrongTool`，不表示现行玩法要求匹配工具。
- 焊接、粘接、硫化、铆死结构不是常规可逆拆装步骤；密封轴承按维修单元处理。游戏不是现实维修教程，制动等安全关键内容需监护人或专业技师把关。
- 首版只用本地存档，不做账号、云同步或跨设备进度。

## 3. 已定的自行车工程参数

| 项目 | 当前基准 |
| --- | --- |
| 模型 ID | `bike.hardtail.27_5.2x10.v1` |
| 车型 | 无品牌 M 码铝合金硬尾山地车，27.5 英寸，2×10 |
| 轮胎 | ETRTO 57-584（27.5 × 2.25）；车轮外径目标 698 mm |
| 轴距 | 1120 mm |
| 前叉 | 120 mm 气压避震，锥形转向管 |
| 前 / 后花鼓 | 15×110 / 12×148 mm Boost；前后均 32 孔 |
| 中轴 | BSA 73 mm 螺纹式 |
| 牙盘 | 36T / 22T 双盘，必须在模型上清晰可辨 |
| 飞轮与链条 | 11–36T 十速飞轮；约 110 节链条与快拆扣 |
| 制动 | 前后双活塞矿物油液压碟刹；前 / 后 180 / 160 mm 六钉碟片 |
| 走线 | 当前采用沿车架布置的外走线，留转向余量；不是悬空直线 |
| 尺寸单位 | BOM 以毫米设计；Blender / 引擎资产换算为米 |

完整 BOM 见 [ENGINEERING_BOM.md](ENGINEERING_BOM.md)；机器数据见 [`bicycle_engineering.json`](../Assets/StreamingAssets/MechanicalCatalog/bicycle_engineering.json)。

### 3.1 数量与拆装边界

- 14 个模块：车架、把组与碗组、避震前叉、前后轮、前后刹车、曲柄与中轴、前后拨链器、链条、脚踏、鞍座与座管、操控与线管。
- 595 个实体是展开重复件后的**模型完整性基线**，不是玩家要连续操作 595 次。后刹车复用前刹车 37 个零件定义，统计和生成时必须展开继承；之前漏算过这层继承，现已进入验证规则。
- 前后轮分别保留 32 根辐条，链条保留链节、飞轮保留 10 片、牙盘保留 2 片，均有稳定身份与模型绑定。但即使在“探索”等级，辐条、链节、重复紧固件和内部小件也**作为合理子总成整体拆装**，不逐个拖动。
- 初期目标是整车所有模块达到维修训练级精密拆解；后续仍可继续改进曲面、铸件、齿片、线管和材质。不能把目前程序化模型宣称为某厂商原始 CAD。

### 3.2 三档规则（最终版本）

| UI 名称 | 内部枚举 | 步骤 | 一次拖动的单位 |
| --- | --- | ---: | --- |
| 简单 | `Simple` | 15 | 整车主要模块 |
| 进阶 | `Standard` | 30 | 按功能系统分组 |
| 探索 | `Advanced` | 45 | 更细的机械子总成，重复小件仍成组 |

内部枚举**保持英文 `Simple / Standard / Advanced`**；用户曾要求中文化内部枚举，随后明确撤回。旧本地存档可删除，不需要为这次文案变化保留兼容；现有存档层仍支持旧自行车键迁移。

三个等级对同一台车均要无遗漏、无重复地覆盖 595 个模型对象。等级不仅调整拆分粒度，也可调整科普深度：简单讲直观作用，进阶讲工作原理，探索讲结构细节或安全边界。**每次呈现的单段零件讲解不超过 50 个汉字**，不把三段长文叠加显示。历史按钮“重听当前零件”统一为“再次讲解”。

## 4. 操作、镜头、界面与反馈约定

1. 默认“旋转视角”：按住车身或空白处单指 / 鼠标左键拖动旋转；轻点零件选中、显示百科并触发讲解；滚轮 / 双指缩放。桌面右键始终可旋转。
2. 点击“拆装零件”：直接拖动机械单元，跟手移动，不应产生手指与零件的明显偏移；靠近所属分类槽时高亮，松手拆入。拖空白处仍可旋转。第二根手指加入或触摸取消时，不能错误提交拆装。
3. 组装时从 3D 分类展示位置拖回整车。现行实现以同一份已拆 ID 集合支持任意顺序装回；**先组装变速、减震等小部件，再组合为整车**的显式子计划仍是后续工作。
4. 左侧可收起的“拆解等级”及三维爆炸视图、右侧可收起的“零件小百科”、底部分类收纳、中央整车视图组成主界面。UI 采用更明亮的青少年配色；可有轻微卡通化的界面语言，但 3D 机械模型保持写实。左右栏和底部区应尽量不侵占整车取景。
5. 顶部“模型”选择位于“旋转视角”左侧；**只显示已交付且通过资源校验的模型**。最新布局已加宽模型文字并保持单行，最右操作提示垂直居中；左右侧栏重复的小标题已去掉，收起箭头保留。
6. “整机归位”重置观察取景而不更改拆装进度；“查看收纳”纳入已拆零件和 3D 展示托盘。右下角半透明“画面平移”提供上下左右和“中”：每次沿屏幕方向平移 48 个设计像素；“中”仅取消平移，保留角度及缩放；整机归位 / 查看收纳复位平移。
7. 全局一键爆炸：按当前等级的 **15 / 30 / 45 个逻辑单元**展开 / 收回；点击单件爆炸：一次仅一个逻辑单元弹开，再点收回。爆炸只供观察，可继续旋转、缩放和平移；不改变拆解计数、已拆集合或存档，切至实际拆装、整机归位、查看收纳或切换等级时复位。
8. 拆装反馈使用简短机械音效，不采用泛用提示音。Windows Editor 已接本地中文系统语音；点击零件、成功拆装和“再次讲解”可播报。关闭讲解会停止当前播报。微信语音 / 已授权音频仍未接入。
9. UI 文字按真实渲染像素绘制：`PixelUILayout` 以 1920×1080 为设计基准，正文约 20 px（4K 约 40 px），不要用 `GUI.matrix` 放大低分辨率字形；Game View 用原生分辨率和 1x 预览检查清晰度。
10. 自行车已有固定 36T / 24T 的原地踩踏演示：仅完整整车可启动，可暂停、继续、结束并在 30–90 转/分间调速；曲柄、脚踏、运转链条、导轮、飞轮及后轮联动，不额外生成脚撑或支架。演示链条显示层绕过两只后拨导轮，保留原有 110 个可拆链节及 595 实体基线。演示状态不写存档；转入拆装、爆炸、收纳或切换模型/等级时复位。

## 5. 已实现的工程与资产

### 5.1 3D 与内容

- `BicycleEngineeringSource.blend` 与生成脚本可重复构建工程车。当前源数据：**595 个有稳定 ID 的对象，其中 585 个网格；119,306 顶点、111,694 面**。
- 已导出 14 个模块 LOD0 FBX，整车 LOD1 为 **14 对象 / 71,305 面**，LOD2 为 **1 对象 / 26,671 面**；现行桌面样片为验证交互会一次加载全部 LOD0，微信目标架构应改为整车 LOD1 + 当前模块 LOD0 按需加载。
- 已针对用户指出的造型问题修正：链条端部圆润并贴合前后链轮、36/22T 双牙盘、前叉导向衬套与阻尼筒同轴、刹把朝把握区域、脚踏为镂空平台、刹车 / 变速线管沿车架走、两颗原悬浮件归位为下管水壶架螺栓、鞍座改为带减压凹槽的曲面。
- 工程 BOM、对象映射、运行资产统计和三档交互目录由脚本生成；**不要直接改生成后的 FBX、模型清单或交互目录**，应改 BOM / 生成脚本后重跑流水线。
- 初期前刹车原型及旧预览可作为历史样片，当前正式入口读取整车目录。仓库里仍可能保留旧原型 FBX，不代表有第二正式车型。

### 5.2 代码与数据架构

- `Assets/Scripts/Domain` 是不依赖引擎的纯 C# 状态机；拆装允许任意顺序，重复操作不改变状态，工具信息不参与校验。
- `MechMasterApp` 组合运行时；`MechanicalModelRegistry` 自动发现 `Assets/Resources/MechanicalCatalog/Models/*.json`；`MechanicalCatalogLoader` 载入各自三级交互目录；`MechanicalModelView`、`MechanicalPartView` 和 `MechanicalPartHitProxy` 将逻辑步骤绑定到真实 FBX 对象与碰撞热区。
- 运行时的 3D 托盘与相机构图以模型包围盒和装配清单驱动；分类超过 14 个时分页。每模型各自存档，按“模型 ID + 等级”隔离。新增合格模型原则上只添加 **1 个模型 JSON + 独立交互目录 + 分件 FBX/Prefab 等资产**，不用给主代码加 `switch`。
- `LocalProgressStore` 当前使用 Unity `PlayerPrefs` 保存所选模型、等级、讲解开关、模式及**准确的已拆步骤 ID 集合**。只存数字不够，因为拆装无固定顺序。爆炸状态不保存。
- UI 目前仍为 IMGUI 原型；长期应迁移到 UGUI 或 UI Toolkit。Windows Editor 讲解由本地 `WindowsSpeechHost` 实现，不可原样打包到微信。
- 自行车动态演示由模型清单的可选 `motion` 配置启用；其他模型尚无运转入口。当前为固定挡位运动学演示，真实换挡和移动端链条显示合批仍待完成。

## 6. 验证现状与可复现命令

截至本文更新，本机执行以下两项均通过：

```powershell
dotnet run --project Tools/Tests/MechMaster.Domain.Tests.csproj -c Release

& 'C:\Program Files\Blender Foundation\Blender 4.5\blender.exe' `
  --background --python Tools/Blender/validate_engineering_bicycle.py
```

.NET 测试为 **13 项全部通过**，覆盖状态机、任意顺序、无工具校验、知识字数、BOM 数量尺寸、15 / 30 / 45 计划、595 个绑定、模型清单发现和自行车动态配置。Blender 校验通过源对象唯一 ID、主要尺寸、外走线、鞍座、双盘、前叉、刹把、镂空脚踏、LOD 与 FBX 往返对象名。本机已完成团结引擎编译 / 导入、Play Mode 启动与动态演示自动验证；**最新 UI、真实手势和真机画面仍需人工复核**，自动测试不等同真机验收。

完整再生顺序：

```text
修改 BOM / 生成脚本
  → generate_engineering_bicycle.py（Source .blend、对象映射、预览）
  → export_engineering_lods.py（14 模块 LOD0、整车 LOD1/LOD2）
  → Generate-BicycleInteractionCatalog.ps1（三档目录）
  → validate_engineering_bicycle.py
  → .NET 测试
  → 团结引擎 Editor 导入、Play 与人工体验
```

运行方法：团结引擎或兼容 Unity 2022 LTS 打开仓库根目录，等待导入，打开 `Assets/Scenes/Main.unity` 后 Play。脚本变更前先退出 Play，编译完再进入；当前原型不保证领域状态能跨热重载恢复。编辑器内已有“机械大师 → 验证文字像素布局”“验证视角与面板交互”等菜单。进一步细节见 [DEVELOPMENT.md](DEVELOPMENT.md)。

## 7. 后续模型的选择与准入

### 7.1 已否决的 F1 路线

早期 F1 占位模型过于抽象，`FormulaCar2026.png` 明确被拒绝。曾讨论 F1 三档约 30 / 45 / 60 步，但这一数字**只属于已放弃的 F1 方案，不是其他新模型的通用要求**。没有真实、可分件且获授权的中高精度模型时，不应保留 F1 假入口或以粗糙占位图冒充已交付。

### 7.2 两个仍待审计的候选

| 候选 | 已知资料 | 当前阻碍 |
| --- | --- | --- |
| **Open Movement OM10 机械腕表机芯** | 官方会员页列 STEP；手动版技术表给出直径 30.80 mm、高 4.50 mm 及轮系参数，可作为 1:1 核验基准 | 未登录取得原始 STEP；电子文件许可可能不同于技术表 CC BY-SA 3.0；尚未验证逐件层级、材质与游戏发行权 |
| **Tomaso V8 爆炸发动机** | Sketchfab 原作者页面称免费可下载、CC BY 4.0，约 220 万三角面、无贴图 / 24 材质 | 未经账号认证取得原始文件；未知具体发动机型号和尺寸，无法证明 1:1；高面数需大幅优化并做移动端验证 |

两者**均未加入模型菜单**。取得原件后要逐项记录来源 URL、作者、下载日期、版本、许可和文件校验值；核实对象层级、零件边界、尺寸、型号依据和小游戏发行 / 改造权限；建立 BOM、稳定 ID、三级计划、中文知识、Source 与移动 LOD，最后真机测包体、内存、帧率与点击精度。若 V8 无法建立真实尺寸依据，应改选有明确型号和技术尺寸的燃油发动机。详见 [MODEL_CANDIDATES.md](References/MODEL_CANDIDATES.md)。

## 8. 待办事项（按依赖排序）

1. **当前样片人工回归**：在 Editor 对三个等级逐一检查模型菜单、顶部文字完整与垂直对齐、左右侧栏、旋转 / 缩放 / 平移、拾取精度、跟手拖动、分类高亮、任意顺序拆装 / 组装、全局及单件爆炸、语音试听、本地存档恢复。最新顶部与侧栏 UI 改动尤其要看实际 Game View。发现问题先修当前自行车，不因自动化通过而跳过。
2. **资产与知识精修**：继续按公开维修资料核实所有 595 个实体的装配关系；为重要子总成增加装配轴、爆炸锚点和停靠点；逐条审校生成文案，尤其制动、转向、承载结构。当前没有专家合作资源，先记录厂商公开维修手册与通用规范，正式发行前争取机械维修和儿童教育审稿。
3. **真正的分层组装**：先完成变速器、避震、轮组等小部件子计划，再将完整子总成装入整车；当前任意顺序装回不等于该功能完成。
4. **刹车油演示**：后续可覆盖三个等级；必须标为虚拟模拟，不是现实维修指导。首版不考核油液规格、工具规格或扭矩。
5. **微信平台**：项目所有者申请 AppID；安装匹配的微信构建支持与开发者工具；实现模型分包 / 缓存、平台本地存储、合法中文字体与讲解音频、生命周期和安全区；把当前全量 LOD0 加载改为按需模块加载、仅启用当前模块热区，优化材质、纹理和 Shader。
6. **真机验收**：中低端手机实测首包 / 分包、首次和二次进入、峰值内存、释放回落、帧率、发热、耗电、Draw Call、纹理显存、GC、双指 / 取消触摸、前后台和音频恢复。未拿到数据不能写“微信性能达标”。
7. **新机械模型**：先取得 OM10 与 V8 的合法可审计原件再做准入判断；若失败，继续搜寻有真实尺寸和可拆层级的非电动交通工具、燃油发动机或钟表轮系。不要因为菜单已有通用化就加入空模型或低精度替身。
8. **UI 工程化**：从当前 IMGUI 原型迁移到适合微信端的正式 UI，同时保留像素清晰度、折叠区、中央构图和手势屏蔽行为；做窄屏、安全区与中文字体许可检查。

## 9. 新对话接手规则与文档导航

- 先读本文件，再读 [README.md](../README.md)、[PRODUCT_SPEC.md](PRODUCT_SPEC.md)、[ARCHITECTURE.md](ARCHITECTURE.md)、[ENGINEERING_BOM.md](ENGINEERING_BOM.md)、[DEVELOPMENT.md](DEVELOPMENT.md)、[SOURCES.md](References/SOURCES.md) 与 [MODEL_CANDIDATES.md](References/MODEL_CANDIDATES.md)。新请求应以用户最新措辞为准；旧文档中的方案不可覆盖后来的明确变更。
- 早期“做完第一步先暂停”已被后续“继续完成第 2～5 步，直到结束”的明确指令取代；目前没有延续中的暂停指令。F1 30 / 45 / 60 步同样只属于已放弃方案。
- 开工前检查 `git status --short`；用户的未提交、已暂存和已提交内容都应保留。继续在 `main` 工作，由用户提交；不要擅自推送、建 PR 或重置工作树。
- 改自行车机械数据时先看 BOM 与生成脚本，保持 595 实体和 15 / 30 / 45 步的自动验证闭环；若经用户批准改变这些基线，必须同步生成数据、测试和文档。
- 新模型的关键约束是**合法原件、真实尺寸、真实分件、写实外观、移动端可运行**；不能仅凭预览图或截图宣称已接入。模型清单格式可复制 [`Bicycle.json`](../Assets/Resources/MechanicalCatalog/Models/Bicycle.json)，但必须使用新 ID。
- 任何第三方模型、字体、音频和图片都要保留来源与许可；不在仓库提交 AppID、密钥、个人账号、`Library/`、`Temp/`、`obj/`、`bin/`、Python 缓存或 Blender 自动备份。
- 对话中曾要求把所给“附件”纳入对应模块，但当前可见文字记录未列出附件文件名和清单；遇到相关扩展时应核对实际原附件与 BOM，不能凭空认定已经全部覆盖。
